<!DOCTYPE html>
<html>
<head>
    <title>..: javascript 04 :..</title>
    <script type="text/javascript">
        function getName() {
                       let name = document.getElementById("txtName").value;
            let password = document.getElementById("passPassword").value;
            
                        if (name == "marc" && password == "Lars123") {
                document.getElementById("name").innerHTML = name + ", you have access !";
            } else {
                document.getElementById("name").innerHTML = "Access Denied: Incorrect name or password !";
            }
        }
    </script>
</head>
<body>

    <label>What is my name</label><br>
    <input type="text" id="txtName"><br>
    
    <label>What is my password</label><br>
    <input type="password" id="passPassword"><br>
    
    <button onClick="getName()">Click to get my name</button>
    
    <div id="name">No name</div>

</body>
</html>